[common]
bind_addr = 0.0.0.0                # Listen on all interfaces
bind_port = 7000                    # Internal port for FRP client connections
dashboard_port = 7500               # Port for the FRP dashboard
dashboard_user = admin              # Dashboard username
dashboard_pwd = admin123           # Dashboard password
token = your_secure_token          # Authentication token shared with the client
vhost_https_port = 443              # The port for HTTPS (exposed to public)

# Other optional settings, such as logging, can be added if necessary
